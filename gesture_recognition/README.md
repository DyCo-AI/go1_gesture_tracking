# Gesture Recognition

ROS Noetic Python package for the operator laptop. It will read the laptop
webcam, classify intentional hand gestures, apply confidence and temporal
validation, and publish a small gesture-intent vocabulary.

The ZED 2i is not an input to this package. The ZED camera node runs separately
on the Go1's Jetson AGX Xavier.

